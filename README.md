# RayTracing on C++
## Compile and run 
### Windows
1) `mkdir build`
2) `make`

### Linux
TODO: compile manually from `src` folder.

## Editing scenery
Check the `src/main.cpp` file - everything is clear there.
